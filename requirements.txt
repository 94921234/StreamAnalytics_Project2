# Producer dependencies - Milestone 2.1
# Install with: pip install -r requirements.txt
# NOTE: live-streaming mode requires the M1 generator (root /generator folder).

azure-eventhub>=5.11.0
python-dotenv>=1.0.0
fastavro>=1.9.0
